# Profile for GenerateCommand: a one-shot publisher, not a server.
# No web server and no consumer -- this process only produces a fault-
# injected stream against a separately-running consumer (the Deployment
# this chart's generator Job is scoped alongside).
spring.main.web-application-type=none
ledgerline.consumer.enabled=false
