# Profile for EmitTransactionCommand: a one-shot publisher, not a server.
# No web server and no consumer -- this process only produces, then exits.
spring.main.web-application-type=none
ledgerline.consumer.enabled=false
